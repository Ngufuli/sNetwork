const initialState = {
  isAuthenticated: false,
  user: {}
  //   hello: "Ngufuli"
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
